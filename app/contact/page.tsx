import ContactForm from "../(common)/contact-form";

export const metadata = {
  title: "Contact | Ben Lister",
};

export default function Contact() {
  return (
    <div>
      <ContactForm />
    </div>
  );
}
